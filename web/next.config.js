/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow Vercel to deploy the web/ subfolder
  // Backend API URL is injected via NEXT_PUBLIC_API_URL env var
}

module.exports = nextConfig
